<!DOCTYPE html>
<html>
<head>
  <title><%= yield :title %></title>
  <link rel="shortcut icon" href="/favicon.ico" />
  <%= stylesheet_link_tag    "application", "projects" %>
  <%= javascript_include_tag "application" %>
  <%= csrf_meta_tags %>
</head>
<body>
  <div id="wrap">
    <header>
      <h1 id="logo"><%= link_to "Startrack", root_path %></h1>
      <% if user_signed_in? %>
      <div class="login-info">
        <%= show_user_avatar(current_user) %> Logged as <%= current_user.email %>. Not you?
        <%= link_to 'Logout', destroy_user_session_path %>
      </div>
      <% else %>
      <div class="login-info">
        <%= link_to 'Register', new_user_registration_path %> ou <%= link_to 'Login', new_user_session_path %>
      </div>
      <% end %>
    </header>

    <hr />

    <%= flash_messages %>

    <%= yield %>

    <footer>
      Startrack is an agile based tracker developed by <%= link_to "RubyOnRio", "http://rubyonrio.org", :target => "_new" %> on version <%= app_version %>
    </footer>
  </div>
</body>
</html>
