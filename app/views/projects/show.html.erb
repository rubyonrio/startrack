<p id="notice"><%= notice %></p>

<p>
  <b>Name:</b>
  <%= @project.name %>
</p>

<p>
  <b>Description:</b>
  <%= @project.description %>
</p>

<h2>Stories:</h2>
<ul>
  <% @project.stories.each do |s| %>
  <li><%= @project.stories.name %></li>
  <% end %>
</ul>


<%= link_to 'Edit', edit_project_path(@project) %> |
<%= link_to 'Back', projects_path %>
 |
<%= link_to 'New Story', new_story_path %>
